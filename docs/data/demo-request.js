/* Demo request form configuration. The form on the portfolio only renders when
   `endpoint` is set. See worker/README.md for the one-time setup. */
window.DEMO_REQUEST = {
  endpoint: "",            // e.g. "https://portfolio-demo-request.<account>.workers.dev"
  turnstileSiteKey: ""     // from the Turnstile widget for ejr1216.github.io
};
